'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
      <button className='border p-2' onClick={() => console.log('click')}> Add to cart </button>
    </div>
  )
}

export default AddToCart
